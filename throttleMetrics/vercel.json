{
  "builds": [{
    "src": "throttleMetrics/wsgi.py",
    "use": "@vercel/python",
    "config": { "maxLambdaSize": "40mb", "runtime": "python3.12" }
  }],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "throttleMetrics/wsgi.py"
    }
  ]
}